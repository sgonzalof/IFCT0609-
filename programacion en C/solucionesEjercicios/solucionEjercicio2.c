#include <stdio.h>
int main()
{
    /* code */
    int lado;
    printf("introduce el lado del cuadrado: ");
    scanf("%d",&lado);
    printf("\nel perimetro del cuadrado es:  %d",4*lado);
    return 0;
}
